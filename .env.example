# Copy this file to .env and fill in real values.
# NEVER commit the real .env file — it is already in .gitignore.

GEMINI_API_KEY=your_google_ai_studio_key_here

# Optional: only needed if we add Groq as a secondary fast text-only provider later
GROQ_API_KEY=

# Local paths (override if running outside Colab)
DATA_DIR=./data
OUTPUT_DIR=./outputs