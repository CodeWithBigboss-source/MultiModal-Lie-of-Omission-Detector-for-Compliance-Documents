# ---- Core LLM / VLM ----
google-genai==1.3.0          # official Gemini SDK (text + vision, single client)
python-dotenv==1.0.1          # load API keys from .env locally / Colab secrets

# ---- Document + image handling ----
pillow==10.4.0
pypdf==4.3.1
pdf2image==1.17.0
pytesseract==0.3.13           # fallback OCR for scanned docs (local, free)
opencv-python-headless==4.10.0.84   # image quality heuristics (blur, resolution)

# ---- Structured data / schema validation ----
pydantic==2.9.2                # enforce claim/verdict schemas on every LLM call

# ---- Orchestration ----
tenacity==9.0.0                # retry/backoff for free-tier 429s

# ---- Backend (Phase 4) ----
fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.12

# ---- Eval / experimentation ----
pandas==2.2.3
scikit-learn==1.5.2            # simple agreement/confusion-matrix metrics on gold set
jupyter==1.1.1